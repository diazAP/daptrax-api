[project]
name = "daptrax-api"
version = "0.1.0"
description = "Backend API for DAPTrax"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "psycopg[binary]",
  "alembic",
  "pydantic-settings",
  "python-dotenv",
]